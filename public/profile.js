const newSubHandler = async (event) => {
    event.preventDefault();

    const name = document.querySelector('#subscription-name').value.trim();
    const price = document.querySelector('#price').value.trim();

    if (name && price) {
        
    }
}